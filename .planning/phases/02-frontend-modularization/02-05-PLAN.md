---
phase: 02-frontend-modularization
plan: 05
type: execute
wave: 4
depends_on:
  - 02-04
files_modified: []
autonomous: false
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03

must_haves:
  truths:
    - "The full 5-step wizard completes without any JavaScript console errors"
    - "File upload and parse (step 1) works — file picker opens, file is accepted, parse runs"
    - "Review step (step 2) populates fields from parsed data and allows editing"
    - "Vendor info and line items (step 3) — add row, edit cells, dup, delete, undo, theme switch all work"
    - "Generate and export (step 4) — quote generation and download work"
    - "All three pytest structural tests remain GREEN after the smoke test"
  artifacts:
    - path: "electron/index.html"
      provides: "Final state — no script block, no inline handlers"
    - path: "electron/js/modules/step1.js"
      provides: "Upload step working"
    - path: "electron/js/modules/step3.js"
      provides: "Line items with event delegation working"
  key_links:
    - from: "npm start"
      to: "full wizard"
      via: "Electron loads index.html, loads script tags in order, bootstraps app"
      pattern: "manual verification"
---

<objective>
Verify the complete wizard works end-to-end after modularization. Human eyes confirm no visual regressions, no broken interactions, and no JavaScript console errors in DevTools.

Purpose: Automated structural tests confirm code organization is correct. This checkpoint confirms the app actually works — that event delegation fires, state updates propagate, and the generate-quote flow completes without errors.
Output: Human sign-off that Phase 2 modularization is regression-free.
</objective>

<execution_context>
@/Users/marcuslucas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcuslucas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-frontend-modularization/02-CONTEXT.md
@.planning/phases/02-frontend-modularization/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run final automated checks</name>
  <files></files>
  <action>
Run the complete pytest suite to confirm all structural tests are GREEN:

```bash
cd /Users/marcuslucas/Desktop/solicitation-quoter
python -m pytest tests/test_arch01.py tests/test_arch02.py tests/test_arch03.py -v
```

Also run the full existing test suite to confirm no regressions in Phase 1 security tests:

```bash
python -m pytest tests/ -v
```

Report the pass/fail count. If any test fails unexpectedly (test_sec0*.py should all pass), fix before proceeding to the human checkpoint.
  </action>
  <verify>
    <automated>cd /Users/marcuslucas/Desktop/solicitation-quoter && python -m pytest tests/ -v 2>&1 | tail -20</automated>
  </verify>
  <done>All pytest tests pass. test_arch01, test_arch02, test_arch03 all GREEN. No existing sec tests regressed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The entire 2100-line index.html JavaScript has been extracted into 7 separate files:
    - electron/js/state.js (global state)
    - electron/js/modules/shared/utils.js (shared helpers)
    - electron/js/modules/shared/theme.js (theme subsystem)
    - electron/js/modules/step1.js (upload step)
    - electron/js/modules/step2.js (review step)
    - electron/js/modules/step3.js (vendor info + line items, event delegation)
    - electron/js/modules/step4.js (generate + export)
    - electron/js/modules/index.js (bootstrapper)

    All 96 inline event handlers removed from index.html HTML. All three structural pytest tests pass GREEN.
  </what-built>
  <how-to-verify>
    1. Run `npm start` from the project root to launch the app.
    2. Open DevTools (View > Toggle Developer Tools) — watch Console for errors during load.
    3. Step 1: Drag a PDF onto the dropzone OR click Browse and select a file. Confirm file name appears and Parse button activates.
    4. Click Parse — confirm the parse request fires (watch Network tab) and results populate in step 2.
    5. Step 2: Edit a field value. Confirm the edited value persists when you navigate away and back.
    6. Step 3: Add 2 line items. Edit description, qty, unit price cells — confirm totals update. Duplicate a row. Delete a row. Click Undo — confirm row restored. Switch theme (Themes button in sidebar) — confirm theme changes.
    7. Step 4: Click Generate Quote — confirm the .docx download prompt appears or file is saved.
    8. Check DevTools Console — confirm zero JavaScript errors throughout the flow.
    9. Confirm the nav items (1/2/3/4 in sidebar) click correctly and highlight the active step.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps work without errors, or describe any issues found (e.g., "step 3 undo broken", "console error: X is not defined")</resume-signal>
</task>

</tasks>

<verification>
- `pytest tests/ -v` — all tests GREEN including test_arch01/02/03
- Human confirms full 5-step wizard completes without JavaScript errors
- Line items add/edit/dup/delete/undo work (event delegation functional)
- Theme switching works (theme.js module functional)
- Vendor profile save/load works (step3 localStorage integration functional)
- Quote generation and download work (step4 + fetch to Flask backend functional)
</verification>

<success_criteria>
- All pytest tests GREEN
- Human-verified wizard completes end-to-end without errors
- Phase 2 ARCH-01, ARCH-02, ARCH-03 all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-modularization/02-05-SUMMARY.md`
</output>
